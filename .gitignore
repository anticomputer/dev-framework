# macOS
.DS_Store

# Editor cruft
*.swp
*~

# Backups written by install.sh / uninstall.sh (into COPILOT_HOME, but ignore here too)
*.bak
*.bak.*

# Local per-repo activation file should NOT live in the framework repo root itself
# (but example configs under examples/ are intentionally committed).
/.dev-framework.yml

# Python
__pycache__/
*.pyc
